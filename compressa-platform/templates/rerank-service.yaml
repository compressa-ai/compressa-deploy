{{- $top := . }}
{{- $spec := .Values.rerank }}
{{- if $spec.enabled }}
{{- /*                                               NOT a misprint, $spec is passed twice intentionally*/}}
{{- include "compressa-platform.service" (dict "top" $top "spec" $spec "name" "rerank" "extras" $spec) }}
{{- end }}